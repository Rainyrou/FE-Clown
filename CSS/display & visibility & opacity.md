`display: none`：元素及其子元素均不可见，从 Render 树中移除，不占据页面空间也不参与页面布局，隐藏元素时触发回流，无法触发与元素绑定的事件处理程序
`visibility: hidden/opacity: 0`：元素及其子元素均不可见但仍存在于 Render 树中，占据页面空间并参与页面布局，隐藏元素时触发重绘，仍可触发与元素绑定的事件处理程序，此外 `opacity: 0` 创建独立于默认文档流的复合图层
