虚拟列表：

- 渲染可视区域内的列表项，当页面滚动时渲染当前可视区域内的 DOM 元素，移除非可视区域的 DOM 元素
- 容器高度 + 列表高度 + 可视区域的起始索引和结束索引 + 列表渲染数量 + item 定高/不定高 + 滚动偏移量
- item 高度 + 容器高度 => 容器可容纳的 DOM 数量 + 起始索引 => 结束索引 => 列表高度
- item 高度 + 数据源 => 列表高度 + 滚动偏移量
- item 高度 + 滚动偏移量 => 起始索引

页面渲染抖动 & 虚拟列表闪烁：

预加载首屏数据 + 缓存和复用 DOM 元素 + 占位符和首屏实际渲染内容位置和尺寸一致、精确计算首屏可视区域避免 DOM 元素位置/尺寸的二次调整和累计布局偏移 + 批量更新 DOM 操作 + 内联首屏 CSS + 减少重绘回流

文件上传：

- 通过 POST 请求提交 Form 表单
- AJAX/Axios/Fetch + FormData 对象
- AJAX/Axios/Fetch + FileReader 转换文件为二进制数据
- 分片上传

分片上传：将大文件转换为二进制数据，通过 `promise.allSettled` 并行上传，首个分片携带大文件 MD5 值和文件分片数，各个分片携带分片 MD5 值和 index，分片上传完毕即发送查询请求，后端返回未接收的分片信息，重传分片
