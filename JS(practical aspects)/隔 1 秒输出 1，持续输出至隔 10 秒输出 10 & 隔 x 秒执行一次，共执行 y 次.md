```js
const logWithDelay = (num, delay) =>
  new Promise((resolve) =>
    setTimeout(() => {
      console.log(num);
      resolve();
    }, delay * 1000)
  );

const execute = async (x = 1, y) => {
  for (let i = 1; i <= y; ++i) await logWithDelay(i, x);
};

execute(1, 10);
```
