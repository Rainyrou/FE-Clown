Axios：基于 Promise，其为函数/对象，初始化时创建实例，配置默认请求参数，合并配置对象，在发送请求前拦截，遍历并执行所有请求拦截器的自定义逻辑，在返回响应前拦截，逆序遍历并执行所有响应拦截器的自定义逻辑，请求/响应拦截器分别在 Promise 链前后端添加，在 Promise 链中任意拦截器出现错误或返回 rejected Promise，中断后续拦截器执行

AJAX

Fetch


