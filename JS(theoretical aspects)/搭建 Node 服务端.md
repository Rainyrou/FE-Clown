1. 初始化项目：`npm init -y`
2. 安装依赖（服务端框架、路由、请求体解析器和请求日志）
3. 在主文件中引入所有依赖并创建服务端应用实例，注册全局中间件（错误处理/日志/跨域/请求体解析），路由层定义路由 + 按业务模块拆分路由 -> 创建路由实例 -> 定义接口，控制器取参 -> 校验参数合法性 -> 调用服务层 -> 返回结果，服务层调用数据层 -> 处理业务逻辑 -> 返回结果，数据层初始化 -> 对数据增删改查 -> 返回结果，启动路由和服务端
