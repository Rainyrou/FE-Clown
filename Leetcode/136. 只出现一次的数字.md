[136. 只出现一次的数字](https://leetcode.cn/problems/single-number/description/?envType=study-plan-v2&envId=top-100-liked)

通过异或「消除」所有出现两次的元素：

1. 任何数与自身异或，结果为 0
2. 任何数与 0 异或，结果仍为该数本身

```javascript
var singleNumber = function(nums) {
  let ans = 0;
  nums.forEach(num => ans ^= num);
  return ans;
};
```
