[50. Pow(x, n)](https://leetcode.cn/problems/powx-n/description/)

```javascript
var myPow = function (x, n) {
  let ans = 1;
  if (n < 0) {
    n = -n;
    x = 1 / x;
  }
  while (n) {
    if (n % 2) ans *= x;
    x *= x;
    n = Math.floor(n / 2);
  }
  return ans;
};
```