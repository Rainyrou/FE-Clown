[和为 K 的子数组](https://leetcode.cn/problems/subarray-sum-equals-k/?envType=study-plan-v2&envId=top-100-liked)

###### 暴力破解 -> 超出时间限制

```javascript
var subarraySum = function(nums, k) {
  let count = 0;
  for(let i = 0;i < nums.length;++i) {
    let curSum = 0;
    for(let j = i;j < nums.length;++j) {
      curSum += nums[j];
      if(curSum === k) ++count;
    }
  }
  return count;
};
```

###### 前缀和 + 哈希表

```javascript
var subarraySum = function(nums, k) {
  const map = new Map();
  map.set(0, 1);
  let prefixSum = 0, ans = 0;
  for(const num of nums) {
    prefixSum += num;
    if(map.has(prefixSum - k)) ans += map.get(prefixSum - k);
    map.set(prefixSum, (map.get(prefixSum) || 0) + 1);
  }
  return ans;
};
```

![[Pasted image 20241101172621.png]]

map 初始化为 `{ 0: 1 }` 以处理某前缀和等于 k 的场景，将其视为有效子数组即计算从数组开始至当前索引的和为 k 的情况
